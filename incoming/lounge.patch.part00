--- a/index.html
+++ b/index.html
@@ -7137,7 +7137,7 @@
   active: false, cool: 0, t: 0, finished: false,
   segments: [], trackLength: 0,
   racers: [], /* { name, color, x, z, speed, lap, finished, ai, isYou, isP2 } */
-  position: 0, /* camera z follows lead human */
+  position: 0, playerX: 0, /* camera z/x follow racer */
   canvas: null, ctx: null,
   segLen: 200, roadW: 2000, lanes: 3, drawDist: 220,
   camH: 1000, camDepth: 0, fov: 100,
@@ -7157,7 +7157,10 @@
   p.camera.x = (p.world.x || 0) - camX;
   p.camera.y = (p.world.y || 0) - camY;
   p.camera.z = (p.world.z || 0) - camZ;
-  if (p.camera.z <= 1) { p.screen.scale = 0; p.screen.x = 0; p.screen.y = 0; p.screen.w = 0; return; }
+  if (p.camera.z <= camDepth) {
+    p.screen.scale = 0; p.screen.x = 0; p.screen.y = 0; p.screen.w = 0;
+    return;
+  }
   p.screen.scale = camDepth / p.camera.z;
   p.screen.x = Math.round(w / 2 + p.screen.scale * p.camera.x * w / 2);
   p.screen.y = Math.round(h / 2 - p.screen.scale * p.camera.y * h / 2);
@@ -7315,7 +7318,12 @@
   raceGame.centrifugal = 0.28;
   raceResetRoad();
   raceGame.racers = raceCollectRacers();
-  raceGame.position = 0;
+  {
+    const you0 = raceGame.racers.find(r => r.isYou) || raceGame.racers[0];
+    const playerZ = raceGame.camH * raceGame.camDepth;
+    raceGame.position = raceUtilIncrease(you0 ? you0.z : 0, -playerZ, raceGame.trackLength);
+    raceGame.playerX = you0 ? you0.x : 0;
+  }
   raceGame.standings = raceGame.racers.map(r => r.name);
   socialReading = true;
   try { setHeroForMiniGame(true); } catch (_) {}
@@ -7384,11 +7392,13 @@
     if (r.isYou) {
       const stickX = (typeof stick !== "undefined" && stick) ? stick.x : 0;
       const stickY = (typeof stick !== "undefined" && stick) ? stick.y : 0;
-      /* P1: WASD / stick only — arrows reserved for same-screen P2 */
-      left = !!(keys["KeyA"]) || stickX < -0.25;
-      right = !!(keys["KeyD"]) || stickX > 0.25;
-      faster = !!(keys["KeyW"]) || stickY < -0.25 || !!keys["Space"];
-      slower = !!(keys["KeyS"]) || stickY > 0.25;
+      /* P1: WASD / stick (also accept lowercase key flags) */
+      left = !!(keys["KeyA"] || keys["KeyA"] === true || keys["a"]) || stickX < -0.25;
+      right = !!(keys["KeyD"] || keys["d"]) || stickX > 0.25;
+      faster = !!(keys["KeyW"] || keys["w"] || keys["Space"] || keys[" "]) || stickY < -0.25;
+      slower = !!(keys["KeyS"] || keys["s"]) || stickY > 0.25;
+      /* gentle auto-cruise so the road always scrolls if idle */
+      if (!faster && !slower && r.speed < raceGame.maxSpeed * 0.35) faster = true;
     } else if (r.isP2) {
       left = !!keys["ArrowLeft"];
       right = !!keys["ArrowRight"];
@@ -7447,9 +7457,13 @@
   const sub = document.getElementById("raceSub");
   if (sub) sub.textContent = "You: WASD/stick · P2: arrows · same screen · ESC quit";
 
-  /* camera follows you (or lead if you finished) */
+  /* camera sits playerZ behind the racer (javascript-racer convention) */
   const camR = (you && !you.finished) ? you : ordered[0];
-  if (camR) raceGame.position = raceUtilIncrease(camR.z, -raceGame.camH * raceGame.camDepth * 0.35, raceGame.trackLength);
+  const playerZ = raceGame.camH * raceGame.camDepth;
+  if (camR) {
+    raceGame.position = raceUtilIncrease(camR.z, -playerZ, raceGame.trackLength);
+    raceGame.playerX = camR.x;
+  }
 
   raceRender();
 
@@ -7464,60 +7478,80 @@
   const cv = raceGame.canvas, ctx = raceGame.ctx;
   if (!cv || !ctx || !raceGame.segments.length) return;
   const width = cv.width, height = cv.height;
+  const segs = raceGame.segments;
   const baseSeg = raceFindSegment(raceGame.position);
   if (!baseSeg) return;
   const camZ = raceGame.position;
-  /* follow player's x for camera */
-  const you = raceGame.racers.find(r => r.isYou) || raceGame.racers[0];
-  const playerX = you ? you.x : 0;
-  const maxy = height;
+  const playerX = (typeof raceGame.playerX === "number")
+    ? raceGame.playerX
+    : ((raceGame.racers.find(r => r.isYou) || raceGame.racers[0] || {}).x || 0);
+  const camDepth = raceGame.camDepth;
+  const camH = raceGame.camH;
+  const roadW = raceGame.roadW;
+  const trackLen = raceGame.trackLength;
+  let maxy = height;
   let x = 0, dx = 0;
 
-  /* sky + hills */
-  const g = ctx.createLinearGradient(0, 0, 0, height * 0.55);
-  g.addColorStop(0, "#1a3a6a"); g.addColorStop(0.55, "#4a90c8"); g.addColorStop(1, "#7ec8e8");
+  /* sky */
+  const g = ctx.createLinearGradient(0, 0, 0, height * 0.5);
+  g.addColorStop(0, "#0b1a33"); g.addColorStop(0.45, "#3a7ab8"); g.addColorStop(1, "#87c4e8");
   ctx.fillStyle = g;
   ctx.fillRect(0, 0, width, height);
-  ctx.fillStyle = "#2d6a3e";
-  ctx.fillRect(0, height * 0.55, width, height * 0.45);
 
   const startIdx = baseSeg.index;
+  /* draw far → near so near overwrites (javascript-racer order) */
   for (let n = 0; n < raceGame.drawDist; n++) {
-    const seg = raceGame.segments[(startIdx + n) % raceGame.segments.length];
-    raceUtilProject(seg.p1, playerX * raceGame.roadW - x, raceGame.camH, camZ, raceGame.camDepth, width, height, raceGame.roadW);
-    raceUtilProject(seg.p2, playerX * raceGame.roadW - x - dx, raceGame.camH, camZ, raceGame.camDepth, width, height, raceGame.roadW);
-    x += dx;
-    dx += seg.curve;
-    if (seg.p1.camera.z <= raceGame.camDepth || seg.p2.screen.y >= maxy || seg.p2.screen.y >= seg.p1.screen.y) continue;
+    const seg = segs[(startIdx + n) % segs.length];
+    const looped = seg.index < startIdx;
+    const camOff = camZ - (looped ? trackLen : 0);
+    raceUtilProject(seg.p1, playerX * roadW - x, camH, camOff, camDepth, width, height, roadW);
+    raceUtilProject(seg.p2, playerX * roadW - x - dx, camH, camOff, camDepth, width, height, roadW);
+
+    if (seg.p1.camera.z <= camDepth ||
+        seg.p2.screen.y >= maxy ||
+        seg.p2.screen.y >= seg.p1.screen.y) {
+      x += dx;
+      dx += seg.curve;
+      continue;
+    }
     raceSegment(ctx, width, raceGame.lanes,
       seg.p1.screen.x, seg.p1.screen.y, seg.p1.screen.w,
       seg.p2.screen.x, seg.p2.screen.y, seg.p2.screen.w,
       seg.color);
+    maxy = seg.p1.screen.y;
+    x += dx;
+    dx += seg.curve;
   }
 
-  /* draw racers (far to near) sorted by z distance from camera */
-  const drawList = raceGame.racers.map(r => {
+  /* cars: far → near, project with loop-aware camera offset */
+  const drawList = [];
+  for (const r of raceGame.racers) {
     let rel = r.z - camZ;
-    if (rel < -raceGame.trackLength / 2) rel += raceGame.trackLength;
-    if (rel > raceGame.trackLength / 2) rel -= raceGame.trackLength;
-    return { r, rel };
-  }).filter(o => o.rel > -raceGame.segLen * 2 && o.rel < raceGame.segLen * raceGame.drawDist)
-    .sort((a, b) => b.rel - a.rel);
+    while (rel < 0) rel += trackLen;
+    while (rel >= trackLen) rel -= trackLen;
+    /* relative ahead of camera (0..trackLen) */
+    if (rel > raceGame.segLen * raceGame.drawDist) continue;
+    drawList.push({ r, rel });
+  }
+  drawList.sort((a, b) => b.rel - a.rel);
 
   for (const { r, rel } of drawList) {
     const seg = raceFindSegment(r.z);
     if (!seg) continue;
-    /* approximate screen pos from segment projection */
-    const p = { world: { x: r.x * raceGame.roadW, y: seg.p1.world.y, z: r.z }, camera: {}, screen: {} };
-    raceUtilProject(p, playerX * raceGame.roadW, raceGame.camH, camZ, raceGame.camDepth, width, height, raceGame.roadW);
-    if (p.camera.z <= raceGame.camDepth) continue;
-    const scale = p.screen.scale * 2200;
-    if (scale < 0.08) continue;
-    raceDrawCar(ctx, p.screen.x, p.screen.y, Math.min(2.2, Math.max(0.15, scale)), r.color, r.name, 0);
+    const looped = r.z < camZ && rel < trackLen / 2;
+    /* more reliable: project using relative depth from camera */
+    const p = {
+      world: { x: r.x * roadW, y: seg.p1.world.y, z: camZ + rel },
+      camera: {}, screen: {}
+    };
+    raceUtilProject(p, playerX * roadW, camH, camZ, camDepth, width, height, roadW);
+    if (p.camera.z <= camDe