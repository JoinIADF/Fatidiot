--- a/index.html
+++ b/index.html
@@ -1668,11 +1668,10 @@
   JUMP · X BOOM · RIFLE · SWAP buttons</span><br>
   Shoot vendings for RX · PCs/servers for complaints · CEO office rants<br>
   Jump out windows to escape<br>
-  <span style="opacity:.75;font-size:13px">Desktop: <b>2</b> Starbase · <b>3</b> Lounge · <b>JOIN MULTIPLAYER</b> for live guests (Playroom)</span></p>
+  <span style="opacity:.75;font-size:13px">Desktop: <b>2</b> Starbase · <b>3</b> Lounge · <b>JOIN MULTIPLAYER</b> for live guests</span></p>
   <div class="modeRow">
     <button type="button" class="modeBtn primary" id="btnPlay">WORKING AT X</button>
     <button type="button" class="modeBtn lounge" id="btnLounge">TIMELINE LOUNGE</button>
-    <button type="button" class="modeBtn live" id="btnLiveLounge">CREATE LIVE LOUNGE</button>
     <button type="button" class="modeBtn mp" id="btnMpLounge">JOIN MULTIPLAYER</button>
     <button type="button" class="modeBtn star" id="btnStarbase">DEFEND STARBASE</button>
   </div>
@@ -12382,7 +12381,6 @@
   };
   bind($("btnPlay"), () => startGame());
   bind($("btnLounge"), () => startGame({ social: true, liveLounge: true }));
-  bind($("btnLiveLounge"), () => openCreateLiveLounge());
   bind($("btnMpLounge"), () => { try { mpOpenJoinModal(); } catch (_) {} });
   bind($("btnStarbase"), () => {
     if (typeof previewLevel2 === "function") previewLevel2();
