--- a/index.html
+++ b/index.html
@@ -7429,7 +7429,7 @@
   /* 30% larger than the original tuxedo operator so he reads next to guests */
   const S = 1.3;
   const BLACK = 0x0d0f14, SATIN = 0x1b1f28, WHITE = 0xf6f7fa,
-        SKIN = 0xe0b090, SNOUT = 0xe89fa8, HAIR = 0x1a120e, STEEL = 0x3a4048;
+        SKIN = 0xd4a07a, SNOUT = 0xe8a0a8, HAIR = 0x2a1c14, STEEL = 0x3a4048;
 
   const add = (parent, geo, matl, x, y, z) => {
     const m = new THREE.Mesh(geo, matl);
@@ -7439,7 +7439,7 @@
     parent.add(m);
     return m;
   };
-  /* ---- legs as groups so they can walk ---- */
+  /* ---- legs ---- */
   const legs = [];
   for (const s of [-1, 1]) {
     const leg = new THREE.Group();
@@ -7453,35 +7453,28 @@
   }
   g.userData.legs = legs;
 
-  /* ---- torso: jacket body sits BACK; shirt only peeks in the V; lapels sit FORWARD
-     (avoids coplanar white-shirt z-fighting / constant chest flicker) ---- */
+  /* ---- tuxedo torso (shirt only in lapel V — no coplanar flicker) ---- */
   const jacketMat = shiny(BLACK, 60);
   const shirtMat = shiny(WHITE, 90, 0xffffff, { polyOff: -2 });
   const lapelMat = shiny(SATIN, 240, 0xffffff, { polyOff: -1 });
-  /* jacket shell — slightly deeper, front face at z ≈ +0.14*S */
   add(g, new THREE.BoxGeometry(0.58 * S, 0.66 * S, 0.28 * S), jacketMat, 0, 1.13 * S, -0.01 * S);
-  /* jacket shoulders / bulk */
   add(g, new THREE.BoxGeometry(0.62 * S, 0.16 * S, 0.3 * S), jacketMat, 0, 1.4 * S, -0.01 * S);
-  /* white shirt: ONLY a narrow strip in the lapel V, clearly in front of jacket front */
   add(g, new THREE.BoxGeometry(0.11 * S, 0.42 * S, 0.04 * S), shirtMat, 0, 1.12 * S, 0.175 * S);
-  /* collar points */
   for (const s of [-1, 1]) {
     const col = add(g, new THREE.BoxGeometry(0.07 * S, 0.05 * S, 0.03 * S), shirtMat, 0.05 * s * S, 1.36 * S, 0.185 * S);
     col.rotation.z = 0.4 * s;
   }
-  /* peaked satin lapels — sit in front of shirt edges, leave center V open */
   for (const s of [-1, 1]) {
     const lap = add(g, new THREE.BoxGeometry(0.14 * S, 0.46 * S, 0.035 * S), lapelMat, 0.12 * s * S, 1.2 * S, 0.2 * S);
     lap.rotation.z = 0.28 * s;
     lap.rotation.y = -0.12 * s;
   }
-  /* bow tie on top of collar, well forward */
-  add(g, new THREE.BoxGeometry(0.08 * S, 0.05 * S, 0.045 * S), shiny(BLACK, 120, 0xffffff, { polyOff: -3 }), 0, 1.42 * S, 0.21 * S);
+  /* classic black bow tie */
+  add(g, new THREE.BoxGeometry(0.09 * S, 0.055 * S, 0.05 * S), shiny(BLACK, 120, 0xffffff, { polyOff: -3 }), 0, 1.42 * S, 0.21 * S);
   for (const s of [-1, 1]) {
-    const w = add(g, new THREE.BoxGeometry(0.1 * S, 0.075 * S, 0.035 * S), shiny(BLACK, 120, 0xffffff, { polyOff: -3 }), 0.08 * s * S, 1.42 * S, 0.21 * S);
-    w.rotation.z = 0.35 * s;
+    const w = add(g, new THREE.BoxGeometry(0.11 * S, 0.08 * S, 0.04 * S), shiny(BLACK, 120, 0xffffff, { polyOff: -3 }), 0.09 * s * S, 1.42 * S, 0.21 * S);
+    w.rotation.z = 0.2 * s;
   }
-  /* pocket square — left breast, on jacket surface */
   add(g, new THREE.BoxGeometry(0.07 * S, 0.03 * S, 0.02 * S), shiny(WHITE, 120, 0xffffff, { polyOff: -2 }), -0.2 * S, 1.28 * S, 0.15 * S);
 
   /* ---- arms ---- */
@@ -7499,7 +7492,7 @@
   }
   g.userData.arms = arms;
 
-  /* ---- silenced pistol ---- */
+  /* ---- pistol ---- */
   const gun = new THREE.Group();
   gun.add(new THREE.Mesh(new THREE.BoxGeometry(0.07 * S, 0.16 * S, 0.05 * S), shiny(STEEL, 180)));
   const grip = new THREE.Mesh(new THREE.BoxGeometry(0.06 * S, 0.14 * S, 0.05 * S), shiny(0x1a1c20, 90));
@@ -7512,61 +7505,85 @@
   g.add(gun);
   g.userData.gun = gun;
 
-  /* ---- head: natural oval (not ball, not box) + wall-street comb-over + smaller snout ---- */
+  /* ---- HEAD matching reference: full regular cut, NO comb-over, NO bald spot ---- */
   const headG = new THREE.Group();
   headG.name = "bondHead";
   headG.position.set(0, 1.7 * S, 0);
-  /* oval skull — slightly taller, less wide than a full sphere */
-  const skull = add(headG, new THREE.SphereGeometry(0.19 * S, 18, 16), shiny(SKIN, 25), 0, 0, 0);
-  skull.scale.set(0.92, 1.12, 0.95);
-  /* soft chin (small, under skull — not a second full head) */
-  const chin = add(headG, new THREE.SphereGeometry(0.1 * S, 12, 10), shiny(SKIN, 25), 0, -0.12 * S, 0.04 * S);
-  chin.scale.set(1.15, 0.7, 1.0);
+  const hairMat = shiny(HAIR, 80);
+  const skinMat = shiny(SKIN, 30);
+
+  /* natural oval face */
+  const skull = add(headG, new THREE.SphereGeometry(0.195 * S, 20, 18), skinMat, 0, 0, 0);
+  skull.scale.set(0.93, 1.1, 0.97);
+  /* chin / jaw */
+  const jaw = add(headG, new THREE.SphereGeometry(0.11 * S, 14, 12), skinMat, 0, -0.13 * S, 0.03 * S);
+  jaw.scale.set(1.2, 0.65, 1.05);
+  /* slight cheek volume */
+  for (const s of [-1, 1]) {
+    const ch = add(headG, new THREE.SphereGeometry(0.07 * S, 10, 8), skinMat, 0.12 * s * S, -0.04 * S, 0.06 * S);
+    ch.scale.set(0.75, 0.9, 0.7);
+  }
   /* ears */
   for (const s of [-1, 1]) {
-    const ear = add(headG, new THREE.SphereGeometry(0.05 * S, 8, 6), shiny(SKIN, 20), 0.175 * s * S, 0.0, -0.02 * S);
-    ear.scale.set(0.55, 1.0, 0.75);
+    const ear = add(headG, new THREE.SphereGeometry(0.048 * S, 8, 6), skinMat, 0.18 * s * S, 0.0, -0.02 * S);
+    ear.scale.set(0.5, 1.05, 0.7);
   }
-
-  /* WALL STREET COMB-OVER: left side part, hair swept right across the crown */
-  const hairMat = shiny(HAIR, 100);
-  /* left side — short / tight (the part side) */
-  add(headG, new THREE.SphereGeometry(0.055 * S, 10, 8), hairMat, -0.155 * S, 0.02 * S, -0.01 * S).scale.set(0.55, 1.1, 0.9);
-  /* crown mass swept to the RIGHT */
-  const comb = add(headG, new THREE.SphereGeometry(0.17 * S, 14, 12), hairMat, 0.04 * S, 0.1 * S, -0.02 * S);
-  comb.scale.set(1.15, 0.55, 1.05);
-  comb.rotation.z = -0.18;
-  comb.rotation.x = -0.15;
-  /* top slick ridge (the comb-over arc) */
-  const ridge = add(headG, new THREE.SphereGeometry(0.14 * S, 12, 10), hairMat, 0.02 * S, 0.14 * S, 0.02 * S);
-  ridge.scale.set(1.25, 0.35, 0.85);
-  ridge.rotation.z = -0.22;
-  /* back of head hair */
-  const back = add(headG, new THREE.SphereGeometry(0.14 * S, 12, 10), hairMat, 0, 0.02 * S, -0.12 * S);
-  back.scale.set(1.05, 0.9, 0.7);
-  /* right side volume (where the comb lands) */
-  const rightVol = add(headG, new THREE.SphereGeometry(0.09 * S, 10, 8), hairMat, 0.15 * S, 0.04 * S, 0.0);
-  rightVol.scale.set(0.7, 1.0, 0.95);
-  /* slight forehead fringe swept right */
-  const fringe = add(headG, new THREE.BoxGeometry(0.22 * S, 0.04 * S, 0.08 * S), hairMat, 0.03 * S, 0.12 * S, 0.12 * S);
-  fringe.rotation.z = -0.15;
-  fringe.rotation.x = 0.2;
-
-  /* eyes — set a bit into the oval */
-  for (const s of [-1, 1]) {
-    add(headG, new THREE.SphereGeometry(0.028 * S, 10, 8), shiny(0x14100c, 60), 0.065 * s * S, 0.025 * S, 0.155 * S);
-    const br = add(headG, new THREE.BoxGeometry(0.08 * S, 0.018 * S, 0.018 * S), hairMat, 0.065 * s * S, 0.07 * S, 0.155 * S);
-    br.rotation.z = -0.08 * s;
+  /* light stubble suggestion (darker jaw strip — subtle) */
+  const stub = add(headG, new THREE.SphereGeometry(0.12 * S, 12, 10), shiny(0xb88968, 20), 0, -0.1 * S, 0.08 * S);
+  stub.scale.set(1.05, 0.55, 0.85);
+
+  /*
+   * FULL REGULAR HAIRCUT (reference):
+   * - Complete coverage over the crown — no bald spot
+   * - Short sides, fuller top with natural forward volume
+   * - Centered, even hairline — NOT a comb-over / side-sweep
+   */
+  /* main crown dome — fully covers top of skull */
+  const crown = add(headG, new THREE.SphereGeometry(0.2 * S, 18, 14, 0, Math.PI * 2, 0, Math.PI * 0.62), hairMat, 0, 0.06 * S, -0.01 * S);
+  crown.scale.set(1.02, 0.95, 1.05);
+  /* extra top fill so no scalp shows through from any angle */
+  const topFill = add(headG, new THREE.SphereGeometry(0.17 * S, 14, 12), hairMat, 0, 0.12 * S, 0.0);
+  topFill.scale.set(1.05, 0.55, 1.0);
+  /* front hairline / bangs volume — centered, natural */
