--- a/index.html
+++ b/index.html
@@ -5394,11 +5394,16 @@
   const sx = (sw - side) / 2;
   const sy = (sh - side) / 2;
   ctx.drawImage(img, sx, sy, side, side, 0, 0, size, size);
-  /* subtle rim so face reads on dark lounge */
+  /* rim so the full-head orb reads cleanly */
   ctx.beginPath();
-  ctx.arc(size / 2, size / 2, size / 2 - 2, 0, Math.PI * 2);
-  ctx.strokeStyle = "rgba(255,255,255,0.35)";
-  ctx.lineWidth = 6;
+  ctx.arc(size / 2, size / 2, size / 2 - 3, 0, Math.PI * 2);
+  ctx.strokeStyle = "rgba(255,255,255,0.55)";
+  ctx.lineWidth = 10;
+  ctx.stroke();
+  ctx.beginPath();
+  ctx.arc(size / 2, size / 2, size / 2 - 3, 0, Math.PI * 2);
+  ctx.strokeStyle = "rgba(0,0,0,0.35)";
+  ctx.lineWidth = 3;
   ctx.stroke();
   const tex = new THREE.CanvasTexture(c);
   tex.colorSpace = THREE.SRGBColorSpace;
@@ -5430,24 +5435,17 @@
     try {
       const tex = await loadPfpTexture(url);
       if (!faceMesh.material) return false;
-      /* dispose old letter map if present */
-      try {
-        const old = faceMesh.material.map;
-        if (old && old !== tex && old.image && old.image.tagName === "CANVAS" && !old.userData?.isPfp) {
-          /* keep letter textures cheap; only dispose if we know it's safe */
-        }
-      } catch (_) {}
       tex.userData = tex.userData || {};
       tex.userData.isPfp = true;
       faceMesh.material.map = tex;
-      faceMesh.material.color.setHex(0xffffff);
+      if (faceMesh.material.color) faceMesh.material.color.setHex(0xffffff);
       faceMesh.material.transparent = true;
       faceMesh.material.toneMapped = false;
       faceMesh.material.needsUpdate = true;
       faceMesh.userData.pfpUrl = url;
       faceMesh.userData.hasPfp = true;
       return true;
-    } catch (_) { /* try next candidate */ }
+    } catch (_) { /* try next */ }
   }
   return false;
 }
@@ -5468,42 +5466,42 @@
   /* phone */
   g.add(box(0.14, 0.24, 0.04, 0x111318, 0.52, 1.25, 0.12));
   g.add(box(0.11, 0.18, 0.01, 0x5ad1ff, 0.52, 1.25, 0.145));
-  /* head + avatar face (letter first, then async X PFP paste) */
-  const head = sph(0.28, 0xe8c4a8, 0, 1.78, 0);
+
+  /* WHOLE HEAD = X PFP (restores prior design — not a small disc on flesh+hair) */
+  const letterMap = avatarTex(u.name || u.handle, u.color);
+  const headMat = new THREE.SpriteMaterial({
+    map: letterMap,
+    transparent: true,
+    depthTest: true,
+    depthWrite: true,
+    toneMapped: false,
+    sizeAttenuation: true,
+  });
+  const head = new THREE.Sprite(headMat);
+  head.name = "XUserPfpHead";
+  head.position.set(0, 1.95, 0);
+  head.scale.set(0.92, 0.92, 1); /* full head diameter */
+  head.userData.noOutline = true;
+  head.userData.isPfpFace = true;
+  head.userData.isPfpHead = true;
+  head.userData._baseScale = 0.92;
   g.add(head);
-  const face = new THREE.Mesh(
-    new THREE.CircleGeometry(0.24, 28),
-    new THREE.MeshBasicMaterial({
-      map: avatarTex(u.name || u.handle, u.color),
-      transparent: true,
-      toneMapped: false,
-      depthWrite: true,
-    })
-  );
-  face.position.set(0, 1.78, 0.275);
-  face.userData.noOutline = true; /* outline shell would eat the photo paste */
-  face.userData.isPfpFace = true;
-  face.name = "XUserFace";
-  g.add(face);
-  g.userData.face = face;
-  /* hair blob — sit slightly back so it doesn't cover the PFP disc */
-  g.add(sph(0.3, 0x2a1c14, 0, 1.96, -0.04));
-  /* NAME above @handle, like a real X profile */
+  g.userData.face = head;
+  g.userData.pfpHead = head;
+
+  /* NAME / handle above the PFP head */
   const nameTag = textSprite(u.name || u.handle, { size: 38, color: "#ffffff", strokeW: 7, maxWidth: 0 });
-  nameTag.position.set(0, 2.86, 0);
+  nameTag.position.set(0, 2.72, 0);
   nameTag.scale.multiplyScalar(0.6);
   g.add(nameTag);
   g.userData.nameTag = nameTag;
   const handleTag = textSprite("@" + u.handle, { size: 30, color: "#8b98a8", strokeW: 6, maxWidth: 0 });
-  handleTag.position.set(0, 2.5, 0);
+  handleTag.position.set(0, 2.42, 0);
   handleTag.scale.multiplyScalar(0.5);
   g.add(handleTag);
   g.userData.handleTag = handleTag;
   addOutline(g, 1.04);
-  /* fire-and-forget PFP load — swaps letter disc when ready */
-  try {
-    applyPfpToFaceMesh(face, u).catch(() => {});
-  } catch (_) {}
+  try { applyPfpToFaceMesh(head, u).catch(() => {}); } catch (_) {}
   return g;
 }
 
@@ -9118,6 +9116,15 @@
   for (const e of enemies) {
     if (e.type === "vc") { updateVCIntruder(e, dt); continue; }
     if (e.type !== "xuser") continue;
+    try {
+      const h = e.mesh && e.mesh.userData && e.mesh.userData.pfpHead;
+      if (h) {
+        h.layers.set(1);
+        h.visible = true;
+        const sc = h.userData._baseScale || 0.92;
+        h.scale.set(sc, sc, 1);
+      }
+    } catch (_) {}
     /* targeted guest freezes in place */
     if (e === socialNear || (socialReading && socialNear === e) || (lockTarget === e && e.type === "xuser")) {
       e.state = "idle";
