--- a/index.html
+++ b/index.html
@@ -1075,24 +1075,48 @@
   body.live-lounge #weapon, body.live-lounge #portalHint { display: none !important; }
   body.live-lounge #wave { opacity: 0.95; }
   #dartPower {
-    position: fixed; left: 50%; bottom: 48px; transform: translateX(-50%);
-    z-index: 85; display: none; width: min(320px, 80vw);
-    background: rgba(8,12,20,0.88); border: 1px solid rgba(124,255,176,0.35);
+    position: fixed; left: 50%; bottom: 36px; transform: translateX(-50%);
+    z-index: 95; display: none; width: min(380px, 88vw);
+    background: rgba(8,12,20,0.92); border: 1px solid rgba(124,255,176,0.4);
     border-radius: 14px; padding: 12px 14px; color: #e8eef8;
-    font: 700 12px/1.3 system-ui, sans-serif; text-align: center;
+    font: 700 12px/1.35 system-ui, sans-serif; text-align: center;
     pointer-events: none;
   }
   #dartPower.show { display: block; }
+  #dartPower.interactive { pointer-events: auto; }
   #dartPower .dp-label { letter-spacing: 0.12em; color: #7CFFB0; margin-bottom: 6px; font-size: 11px; }
   #dartPower .dp-track {
-    height: 14px; border-radius: 8px; background: #1a2230;
+    height: 16px; border-radius: 8px; background: #1a2230;
     border: 1px solid #3a4558; overflow: hidden;
   }
   #dartPower .dp-fill {
     height: 100%; width: 0%; background: #ffd94a;
-    transition: width 0.05s linear;
+    transition: width 0.04s linear;
   }
   #dartPower .dp-hint { margin-top: 8px; color: #8b98a8; font-weight: 600; font-size: 11px; }
+  #dartPower .dp-score {
+    margin-top: 6px; font: 800 18px/1.2 system-ui, sans-serif; color: #e8eef8;
+  }
+  #dartPower .dp-modes {
+    display: flex; gap: 8px; justify-content: center; margin: 10px 0 4px; flex-wrap: wrap;
+  }
+  #dartPower .dp-mode {
+    pointer-events: auto; cursor: pointer;
+    border: 1.5px solid #3a4558; background: #141a24; color: #e8eef8;
+    font: 800 12px/1 system-ui, sans-serif; padding: 10px 14px; border-radius: 10px;
+  }
+  #dartPower .dp-mode:hover { border-color: #7CFFB0; color: #7CFFB0; }
+  #dartPower .dp-mode.on { border-color: #7CFFB0; background: rgba(20,50,36,0.9); color: #7CFFB0; }
+  #dartHud {
+    position: fixed; top: 14px; left: 50%; transform: translateX(-50%);
+    z-index: 94; display: none; min-width: 220px;
+    background: rgba(8,12,20,0.88); border: 1px solid rgba(255,255,255,0.12);
+    border-radius: 12px; padding: 8px 14px; color: #e8eef8;
+    font: 700 12px/1.4 system-ui, sans-serif; text-align: center;
+    pointer-events: none;
+  }
+  #dartHud.show { display: block; }
+  #dartHud b { color: #7CFFB0; }
   body.live-lounge #score { opacity: 0.9; }
 
   /* —— Lounge Activated / Favorites panel —— */
@@ -6650,11 +6674,9 @@
   dartGame.league = true;
   dartGame.player = 0;
   dartGame.pScores = [0, 0, 0, 0];
-  dartGame.total = 0;
-  dartGame.throwsLeft = 12;
+  dartGame.mode = dartGame.mode || "201";
   startDartGame();
-  dartGame.throwsLeft = 12;
-  showMsg("🎯 DARTS LEAGUE — 12 darts · hold LMB power · mouse aim · 4 seats");
+  showMsg("🎯 DARTS — pick 201 or Cricket · hold LMB for power · mouse aims");
 }
 try { window.startDartLeague = startDartLeague; window.startDartGame = startDartGame; } catch (_) {}
 
@@ -7127,85 +7149,119 @@
     g.add(rt);
     g.userData.gameSpots.push({ kind: "race", pos: new THREE.Vector3(tx, 0, tz - 5.4), seats: 4 });
 
-    /* darts on EAST WALL — alley with oche, not mid-room */
+    /* darts — FLUSH on EAST WALL (not mid-room). Single mesh board, no sprites. */
     try {
-      const wallX = W / 2 - 0.35;
-      const boardZ = 8;
-      const boardY = 1.72;
-      const boardR = 0.95;
-      const ocheX = wallX - 6.2;
-      g.add(box(0.22, 2.6, 2.8, 0x1a120c, wallX, 1.75, boardZ));
-      g.add(box(0.14, 2.2, 2.4, 0x2a1a10, wallX - 0.1, 1.75, boardZ));
-      const boardTex = canvasTex(512, 512, (ctx, cw, ch) => {
+      const wallX = W / 2 - 0.28;
+      const boardZ = -6.5; /* north-east corner wall run — clear of dance floor */
+      const boardY = 1.73; /* official ~5'8" eye height */
+      const boardR = 0.228 * 2.0; /* ~18" face, slightly scaled for readability */
+      const throwDist = 3.9; /* oche distance along -X from board face */
+      const boardX = wallX - 0.06; /* flush to wall face */
+      const ocheX = boardX - throwDist;
+      /* wall cabinet / surround */
+      g.add(box(0.18, 2.4, 2.2, 0x1a120c, wallX, 1.7, boardZ));
+      g.add(box(0.1, 2.0, 1.9, 0x2a1a10, wallX - 0.08, 1.7, boardZ));
+      /* proper dartboard texture: 20 sectors, wire rings, numbers */
+      const SECTORS = [6,13,4,18,1,20,5,12,9,14,11,8,16,7,19,3,17,2,15,10];
+      const boardTex = canvasTex(1024, 1024, (ctx, cw, ch) => {
         const cx = cw / 2, cy = ch / 2, R = cw * 0.46;
-        ctx.fillStyle = "#12100c"; ctx.fillRect(0, 0, cw, ch);
+        ctx.fillStyle = "#0c0a08"; ctx.fillRect(0, 0, cw, ch);
+        /* sisal wedges */
         for (let i = 0; i < 20; i++) {
-          const a0 = (i / 20) * Math.PI * 2 - Math.PI / 20;
-          const a1 = ((i + 1) / 20) * Math.PI * 2 - Math.PI / 20;
+          const a0 = (i / 20) * Math.PI * 2 - Math.PI / 2 - Math.PI / 20;
+          const a1 = a0 + Math.PI / 10;
           ctx.beginPath(); ctx.moveTo(cx, cy); ctx.arc(cx, cy, R, a0, a1); ctx.closePath();
-          ctx.fillStyle = i % 2 ? "#1f4a30" : "#101010"; ctx.fill();
+          ctx.fillStyle = i % 2 ? "#111814" : "#0e3d28"; ctx.fill();
+          /* double ring band color */
+          ctx.beginPath(); ctx.arc(cx, cy, R * 0.97, a0, a1); ctx.arc(cx, cy, R * 0.90, a1, a0, true); ctx.closePath();
+          ctx.fillStyle = i % 2 ? "#c41e1e" : "#0a8a3a"; ctx.fill();
+          /* triple ring */
+          ctx.beginPath(); ctx.arc(cx, cy, R * 0.62, a0, a1); ctx.arc(cx, cy, R * 0.55, a1, a0, true); ctx.closePath();
+          ctx.fillStyle = i % 2 ? "#c41e1e" : "#0a8a3a"; ctx.fill();
+          /* number */
+          const am = (a0 + a1) / 2;
+          const nx = cx + Math.cos(am) * R * 1.08;
+          const ny = cy + Math.sin(am) * R * 1.08;
+          ctx.fillStyle = "#e8e0c8";
+          ctx.font = "bold 36px system-ui, sans-serif";
+          ctx.textAlign = "center"; ctx.textBaseline = "middle";
+          ctx.fillText(String(SECTORS[i]), nx, ny);
         }
-        [[0.95, "#d4af37"], [0.62, "#a01818"], [0.38, "#d4af37"], [0.12, "#a01818"]].forEach(([fr, col]) => {
-          ctx.beginPath(); ctx.arc(cx, cy, R * fr, 0, Math.PI * 2);
-          ctx.strokeStyle = col; ctx.lineWidth = R * 0.05; ctx.stroke();
-        });
-        ctx.beginPath(); ctx.arc(cx, cy, R * 0.05, 0, Math.PI * 2);
-        ctx.fillStyle = "#d4af37"; ctx.fill();
-        ctx.strokeStyle = "#d4af37"; ctx.lineWidth = 8;
+        /* outer wire */
+        ctx.strokeStyle = "#c8a84a"; ctx.lineWidth = 6;
         ctx.beginPath(); ctx.arc(cx, cy, R, 0, Math.PI * 2); ctx.stroke();
+        /* rings */
+        [[0.97, 0.90], [0.62, 0.55], [0.16, 0.08]].forEach(([o, i]) => {
+          ctx.strokeStyle = "#b8a060"; ctx.lineWidth = 3;
+          ctx.beginPath(); ctx.arc(cx, cy, R * o, 0, Math.PI * 2); ctx.stroke();
+          ctx.beginPath(); ctx.arc(cx, cy, R * i, 0, Math.PI * 2); ctx.stroke();
+        });
+        /* outer bull 25 */
+        ctx.fillStyle = "#0a8a3a";
+        ctx.beginPath(); ctx.arc(cx, cy, R * 0.16, 0, Math.PI * 2); ctx.fill();
+        /* bull 50 */
+        ctx.fillStyle = "#c41e1e";
+        ctx.beginPath(); ctx.arc(cx, cy, R * 0.08, 0, Math.PI * 2); ctx.fill();
       });
-      /* Plane facing into room (-X): rotate Y 90° so +Z local faces -X world... 
-         Plane default faces +Z. rotation.y = +PI/2 faces -X. */
-      const boardPos = new THREE.Vector3(ocheX + 2.8, boardY, boardZ); /* closer to thrower, clear of wall */
-      const board = new THREE.Mesh(
-        new THREE.PlaneGeometry(boardR * 2.2, boardR * 2.2),
-        new THREE.MeshBasicMaterial({ map: boardTex, toneMapped: false, side: THREE.DoubleSide })
