--- a/index.html
+++ b/index.html
@@ -7149,118 +7149,122 @@
     g.add(rt);
     g.userData.gameSpots.push({ kind: "race", pos: new THREE.Vector3(tx, 0, tz - 5.4), seats: 4 });
 
-    /* darts — FLUSH on EAST WALL (not mid-room). Single mesh board, no sprites. */
+    /* darts — large bright board on EAST WALL, clearly in-room (never buried) */
     try {
-      const wallX = W / 2 - 0.28;
-      const boardZ = -6.5; /* north-east corner wall run — clear of dance floor */
-      const boardY = 1.73; /* official ~5'8" eye height */
-      const boardR = 0.228 * 2.0; /* ~18" face, slightly scaled for readability */
-      const throwDist = 3.9; /* oche distance along -X from board face */
-      const boardX = wallX - 0.06; /* flush to wall face */
+      const wallX = W / 2; /* 24 — visual east wall center */
+      const boardZ = -2.5; /* aligned with Game Arena so players see it */
+      const boardY = 1.85;
+      const boardR = 0.95; /* large, readable from oche */
+      const throwDist = 4.2;
+      /* sit clearly inside the room, in front of plaster (wall face ~23.86) */
+      const boardX = wallX - 0.55;
       const ocheX = boardX - throwDist;
-      /* wall cabinet / surround */
-      g.add(box(0.18, 2.4, 2.2, 0x1a120c, wallX, 1.7, boardZ));
-      g.add(box(0.1, 2.0, 1.9, 0x2a1a10, wallX - 0.08, 1.7, boardZ));
-      /* proper dartboard texture: 20 sectors, wire rings, numbers */
+      /* backboard / cabinet (behind the face) */
+      const back = box(0.16, boardR * 2.5, boardR * 2.5, 0x1a120c, boardX + 0.12, boardY, boardZ);
+      g.add(back);
+      const trim = box(0.08, boardR * 2.65, boardR * 2.65, 0x3a2818, boardX + 0.08, boardY, boardZ);
+      g.add(trim);
       const SECTORS = [6,13,4,18,1,20,5,12,9,14,11,8,16,7,19,3,17,2,15,10];
       const boardTex = canvasTex(1024, 1024, (ctx, cw, ch) => {
-        const cx = cw / 2, cy = ch / 2, R = cw * 0.46;
-        ctx.fillStyle = "#0c0a08"; ctx.fillRect(0, 0, cw, ch);
-        /* sisal wedges */
+        const cx = cw / 2, cy = ch / 2, R = cw * 0.42;
+        ctx.fillStyle = "#1a1410"; ctx.fillRect(0, 0, cw, ch);
         for (let i = 0; i < 20; i++) {
           const a0 = (i / 20) * Math.PI * 2 - Math.PI / 2 - Math.PI / 20;
           const a1 = a0 + Math.PI / 10;
           ctx.beginPath(); ctx.moveTo(cx, cy); ctx.arc(cx, cy, R, a0, a1); ctx.closePath();
-          ctx.fillStyle = i % 2 ? "#111814" : "#0e3d28"; ctx.fill();
-          /* double ring band color */
-          ctx.beginPath(); ctx.arc(cx, cy, R * 0.97, a0, a1); ctx.arc(cx, cy, R * 0.90, a1, a0, true); ctx.closePath();
-          ctx.fillStyle = i % 2 ? "#c41e1e" : "#0a8a3a"; ctx.fill();
-          /* triple ring */
-          ctx.beginPath(); ctx.arc(cx, cy, R * 0.62, a0, a1); ctx.arc(cx, cy, R * 0.55, a1, a0, true); ctx.closePath();
-          ctx.fillStyle = i % 2 ? "#c41e1e" : "#0a8a3a"; ctx.fill();
-          /* number */
+          ctx.fillStyle = i % 2 ? "#0d1f14" : "#1a6b3c"; ctx.fill();
+          /* doubles */
+          ctx.beginPath(); ctx.arc(cx, cy, R * 0.98, a0, a1); ctx.arc(cx, cy, R * 0.90, a1, a0, true); ctx.closePath();
+          ctx.fillStyle = i % 2 ? "#e02020" : "#12a848"; ctx.fill();
+          /* triples */
+          ctx.beginPath(); ctx.arc(cx, cy, R * 0.62, a0, a1); ctx.arc(cx, cy, R * 0.54, a1, a0, true); ctx.closePath();
+          ctx.fillStyle = i % 2 ? "#e02020" : "#12a848"; ctx.fill();
           const am = (a0 + a1) / 2;
-          const nx = cx + Math.cos(am) * R * 1.08;
-          const ny = cy + Math.sin(am) * R * 1.08;
-          ctx.fillStyle = "#e8e0c8";
-          ctx.font = "bold 36px system-ui, sans-serif";
+          ctx.fillStyle = "#f5ecd0";
+          ctx.font = "bold 42px Arial, sans-serif";
           ctx.textAlign = "center"; ctx.textBaseline = "middle";
-          ctx.fillText(String(SECTORS[i]), nx, ny);
+          ctx.fillText(String(SECTORS[i]), cx + Math.cos(am) * R * 1.1, cy + Math.sin(am) * R * 1.1);
         }
-        /* outer wire */
-        ctx.strokeStyle = "#c8a84a"; ctx.lineWidth = 6;
+        ctx.strokeStyle = "#e0c060"; ctx.lineWidth = 10;
         ctx.beginPath(); ctx.arc(cx, cy, R, 0, Math.PI * 2); ctx.stroke();
-        /* rings */
-        [[0.97, 0.90], [0.62, 0.55], [0.16, 0.08]].forEach(([o, i]) => {
-          ctx.strokeStyle = "#b8a060"; ctx.lineWidth = 3;
+        [[0.98, 0.90], [0.62, 0.54]].forEach(([o, inn]) => {
+          ctx.strokeStyle = "#d4b060"; ctx.lineWidth = 4;
           ctx.beginPath(); ctx.arc(cx, cy, R * o, 0, Math.PI * 2); ctx.stroke();
-          ctx.beginPath(); ctx.arc(cx, cy, R * i, 0, Math.PI * 2); ctx.stroke();
+          ctx.beginPath(); ctx.arc(cx, cy, R * inn, 0, Math.PI * 2); ctx.stroke();
         });
-        /* outer bull 25 */
-        ctx.fillStyle = "#0a8a3a";
+        ctx.fillStyle = "#12a848";
         ctx.beginPath(); ctx.arc(cx, cy, R * 0.16, 0, Math.PI * 2); ctx.fill();
-        /* bull 50 */
-        ctx.fillStyle = "#c41e1e";
-        ctx.beginPath(); ctx.arc(cx, cy, R * 0.08, 0, Math.PI * 2); ctx.fill();
+        ctx.fillStyle = "#e02020";
+        ctx.beginPath(); ctx.arc(cx, cy, R * 0.07, 0, Math.PI * 2); ctx.fill();
+        ctx.strokeStyle = "#f0d878"; ctx.lineWidth = 5;
+        ctx.beginPath(); ctx.arc(cx, cy, R * 0.16, 0, Math.PI * 2); ctx.stroke();
       });
+      boardTex.anisotropy = 8;
       const boardGrp = new THREE.Group();
       boardGrp.name = "DartBoard";
-      /* thick cork body */
+      /* cork cylinder body */
       const cork = new THREE.Mesh(
-        new THREE.CylinderGeometry(boardR * 1.02, boardR * 1.02, 0.08, 48),
-        new THREE.MeshStandardMaterial({ color: 0x2a2018, roughness: 0.9 })
+        new THREE.CylinderGeometry(boardR, boardR, 0.1, 48),
+        new THREE.MeshBasicMaterial({ color: 0x3a2818 })
       );
       cork.rotation.z = Math.PI / 2;
       boardGrp.add(cork);
-      /* face — plane on room side (-X) */
+      /* FACE — unlit, double-sided, always visible */
       const face = new THREE.Mesh(
-        new THREE.PlaneGeometry(boardR * 2.05, boardR * 2.05),
-        new THREE.MeshBasicMaterial({ map: boardTex, toneMapped: false })
+        new THREE.CircleGeometry(boardR * 0.98, 64),
+        new THREE.MeshBasicMaterial({
+          map: boardTex, toneMapped: false, side: THREE.DoubleSide
+        })
       );
       face.rotation.y = -Math.PI / 2;
-      face.position.x = -0.045;
+      face.position.x = -0.06;
       face.userData.dartBoard = true;
       boardGrp.add(face);
-      /* gold wire ring */
+      /* bright gold wire ring */
       const ring = new THREE.Mesh(
-        new THREE.TorusGeometry(boardR * 1.03, 0.018, 8, 48),
-        new THREE.MeshStandardMaterial({ color: 0xd4af37, metalness: 0.7, roughness: 0.35 })
+        new THREE.TorusGeometry(boardR * 1.01, 0.035, 10, 64),
+        new THREE.MeshBasicMaterial({ color: 0xffd94a, toneMapped: false })
       );
       ring.rotation.y = Math.PI / 2;
-      ring.position.x = -0.05;
+      ring.position.x = -0.06;
       boardGrp.add(ring);
       boardGrp.position.set(boardX, boardY, boardZ);
       g.add(boardGrp);
       g.userData.dartBoardMesh = boardGrp;
-      /* light on board only */
-      const dartLight = new THREE.SpotLight(0xfff2e0, 18, 14, 0.55, 0.4);
-      dartLight.position.set(boardX - 1.8, boardY + 1.4, boardZ);
-      dartLight.target.position.set(boardX, boardY, boardZ);
-      g.add(dartLight); g.add(dartLight.target);
-      g.userData.dartBoardPos = new THREE.Vector3(boardX - 0.05, boardY, boardZ);
+      /* strong unlit key light so board pops even in dark lounge */
+      const key = new THREE.PointLight(0xfff0dd, 40, 16);
+      key.position.set(boardX - 1.4, boardY + 0.6, boardZ);
+      g.add(key);
+      const rim = new THREE.PointLight(0x7CFFB0, 12, 10);
+      rim.position.set(boardX - 0.8, boardY + 1.8, boardZ + 1.2);
+      g.add(rim);
+      g.userData.dartBoardPos = new THREE.Vector3(boardX - 0.08, boardY, boardZ);
