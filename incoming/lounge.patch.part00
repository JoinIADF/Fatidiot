--- a/index.html
+++ b/index.html
@@ -7421,78 +7421,108 @@
 function buildBondPig() {
   const g = new THREE.Group();
   g.name = "PigRoyale";
+  /* 30% larger than the original tuxedo operator so he reads next to guests */
+  const S = 1.3;
   const BLACK = 0x0d0f14, SATIN = 0x1b1f28, WHITE = 0xf6f7fa,
         SKIN = 0xe0b090, SNOUT = 0xe89fa8, HAIR = 0x241a14, STEEL = 0x3a4048;
 
-  const add = (geo, matl, x, y, z) => {
+  const add = (parent, geo, matl, x, y, z) => {
     const m = new THREE.Mesh(geo, matl);
     m.position.set(x, y, z);
     m.castShadow = true;
-    g.add(m);
+    m.receiveShadow = true;
+    parent.add(m);
     return m;
   };
-  /* ---- legs: tapered tuxedo trousers with satin side stripe ---- */
+  /* ---- legs as groups so they can walk (stored on userData) ---- */
+  const legs = [];
   for (const s of [-1, 1]) {
-    add(new THREE.BoxGeometry(0.19, 0.78, 0.22), shiny(BLACK, 40), 0.13 * s, 0.42, 0);
-    add(new THREE.BoxGeometry(0.02, 0.78, 0.03), shiny(SATIN, 220), 0.225 * s, 0.42, 0);
-    /* patent-leather shoes — highest gloss on the model */
-    const shoe = add(new THREE.BoxGeometry(0.22, 0.09, 0.4), shiny(0x05060a, 300), 0.13 * s, 0.05, 0.08);
-    shoe.geometry = shoe.geometry.clone();
-  }
-  /* ---- torso: jacket, shirt, lapels, bow tie ---- */
-  add(new THREE.BoxGeometry(0.58, 0.66, 0.3), shiny(BLACK, 60), 0, 1.13, 0);
-  add(new THREE.BoxGeometry(0.2, 0.6, 0.06), shiny(WHITE, 90), 0, 1.15, 0.15);   /* shirt placket */
-  for (const s of [-1, 1]) {                                                      /* peaked satin lapels */
-    const lap = add(new THREE.BoxGeometry(0.16, 0.5, 0.05), shiny(SATIN, 240), 0.12 * s, 1.2, 0.155);
+    const leg = new THREE.Group();
+    leg.name = s < 0 ? "bondLegL" : "bondLegR";
+    leg.position.set(0.13 * s * S, 0, 0);
+    add(leg, new THREE.BoxGeometry(0.19 * S, 0.78 * S, 0.22 * S), shiny(BLACK, 40), 0, 0.42 * S, 0);
+    add(leg, new THREE.BoxGeometry(0.02 * S, 0.78 * S, 0.03 * S), shiny(SATIN, 220), 0.095 * s * S, 0.42 * S, 0);
+    /* patent shoes */
+    add(leg, new THREE.BoxGeometry(0.22 * S, 0.09 * S, 0.4 * S), shiny(0x05060a, 300), 0, 0.05 * S, 0.08 * S);
+    g.add(leg);
+    legs.push(leg);
+  }
+  g.userData.legs = legs;
+  /* ---- torso: jacket, shirt, lapels, bow (single layer — no z-fighting extras) ---- */
+  add(g, new THREE.BoxGeometry(0.58 * S, 0.66 * S, 0.3 * S), shiny(BLACK, 60), 0, 1.13 * S, 0);
+  add(g, new THREE.BoxGeometry(0.2 * S, 0.58 * S, 0.05 * S), shiny(WHITE, 90), 0, 1.14 * S, 0.155 * S);
+  for (const s of [-1, 1]) {
+    const lap = add(g, new THREE.BoxGeometry(0.16 * S, 0.48 * S, 0.04 * S), shiny(SATIN, 240), 0.13 * s * S, 1.2 * S, 0.16 * S);
     lap.rotation.z = 0.22 * s;
   }
-  add(new THREE.BoxGeometry(0.07, 0.05, 0.05), shiny(BLACK, 120), 0, 1.44, 0.18); /* bow knot */
+  add(g, new THREE.BoxGeometry(0.08 * S, 0.055 * S, 0.05 * S), shiny(BLACK, 120), 0, 1.46 * S, 0.185 * S);
   for (const s of [-1, 1]) {
-    const w = add(new THREE.BoxGeometry(0.09, 0.08, 0.04), shiny(BLACK, 120), 0.075 * s, 1.44, 0.18);
+    const w = add(g, new THREE.BoxGeometry(0.1 * S, 0.08 * S, 0.04 * S), shiny(BLACK, 120), 0.08 * s * S, 1.46 * S, 0.185 * S);
     w.rotation.z = 0.35 * s;
   }
-  add(new THREE.BoxGeometry(0.07, 0.03, 0.02), shiny(WHITE, 120), -0.19, 1.26, 0.16); /* pocket square */
-  /* ---- arms ---- */
-  const armL = add(new THREE.BoxGeometry(0.16, 0.6, 0.18), shiny(BLACK, 60), -0.37, 1.12, 0.02);
-  armL.rotation.z = 0.06;
-  const armR = add(new THREE.BoxGeometry(0.16, 0.6, 0.18), shiny(BLACK, 60), 0.37, 1.12, 0.06);
-  armR.rotation.z = -0.06;
-  for (const s of [-1, 1]) add(new THREE.BoxGeometry(0.17, 0.05, 0.19), shiny(WHITE, 90), 0.37 * s, 0.83, 0.04); /* cuffs */
-  const handR = add(new THREE.SphereGeometry(0.075, 8, 6), shiny(SKIN, 30), 0.38, 0.75, 0.07);
-  add(new THREE.SphereGeometry(0.075, 8, 6), shiny(SKIN, 30), -0.38, 0.75, 0.03);
-  /* ---- silenced pistol, muzzle down ---- */
+  add(g, new THREE.BoxGeometry(0.07 * S, 0.03 * S, 0.02 * S), shiny(WHITE, 120), -0.2 * S, 1.28 * S, 0.165 * S);
+  /* ---- arms (groups for idle sway) ---- */
+  const arms = [];
+  for (const s of [-1, 1]) {
+    const arm = new THREE.Group();
+    arm.name = s < 0 ? "bondArmL" : "bondArmR";
+    arm.position.set(0.37 * s * S, 1.42 * S, 0.02 * S);
+    add(arm, new THREE.BoxGeometry(0.16 * S, 0.6 * S, 0.18 * S), shiny(BLACK, 60), 0, -0.3 * S, 0);
+    add(arm, new THREE.BoxGeometry(0.17 * S, 0.05 * S, 0.19 * S), shiny(WHITE, 90), 0, -0.59 * S, 0.02 * S);
+    add(arm, new THREE.SphereGeometry(0.075 * S, 10, 8), shiny(SKIN, 30), 0, -0.67 * S, 0.03 * S);
+    arm.rotation.z = 0.06 * s;
+    g.add(arm);
+    arms.push(arm);
+  }
+  g.userData.arms = arms;
+  /* ---- silenced pistol on right hand ---- */
   const gun = new THREE.Group();
-  gun.add(new THREE.Mesh(new THREE.BoxGeometry(0.07, 0.16, 0.05), shiny(STEEL, 180)));            /* slide */
-  const grip = new THREE.Mesh(new THREE.BoxGeometry(0.06, 0.14, 0.05), shiny(0x1a1c20, 90));
-  grip.position.set(0, 0.06, -0.06); grip.rotation.x = 0.35;
+  gun.add(new THREE.Mesh(new THREE.BoxGeometry(0.07 * S, 0.16 * S, 0.05 * S), shiny(STEEL, 180)));
+  const grip = new THREE.Mesh(new THREE.BoxGeometry(0.06 * S, 0.14 * S, 0.05 * S), shiny(0x1a1c20, 90));
+  grip.position.set(0, 0.06 * S, -0.06 * S); grip.rotation.x = 0.35;
   gun.add(grip);
-  const sil = new THREE.Mesh(new THREE.CylinderGeometry(0.035, 0.035, 0.34, 12), shiny(0x23272e, 200));
-  sil.position.y = -0.24;
+  const sil = new THREE.Mesh(new THREE.CylinderGeometry(0.035 * S, 0.035 * S, 0.34 * S, 12), shiny(0x23272e, 200));
+  sil.position.y = -0.24 * S;
   gun.add(sil);
-  gun.position.set(0.4, 0.66, 0.1);
+  gun.position.set(0.4 * S, 0.66 * S, 0.1 * S);
   g.add(gun);
   g.userData.gun = gun;
-  /* ---- head: human, slicked hair, and the snout ---- */
-  const head = add(new THREE.BoxGeometry(0.34, 0.36, 0.32), shiny(SKIN, 25), 0, 1.66, 0);
-  head.geometry = head.geometry.clone();
-  add(new THREE.BoxGeometry(0.36, 0.12, 0.34), shiny(HAIR, 110), 0, 1.86, -0.01);   /* slicked top */
-  add(new THREE.BoxGeometry(0.37, 0.1, 0.1), shiny(HAIR, 110), 0, 1.8, -0.14);      /* back */
-  for (const s of [-1, 1]) add(new THREE.BoxGeometry(0.04, 0.16, 0.2), shiny(HAIR, 110), 0.175 * s, 1.72, -0.02);
-  for (const s of [-1, 1]) {                                                        /* eyes + brows */
-    add(new THREE.SphereGeometry(0.028, 8, 6), shiny(0x14100c, 60), 0.085 * s, 1.7, 0.16);
-    const br = add(new THREE.BoxGeometry(0.09, 0.022, 0.02), shiny(HAIR, 60), 0.085 * s, 1.75, 0.16);
+  /* ---- ROUND head (sphere) + hair + snout — no overlapping cubes that z-fight ---- */
+  const headG = new THREE.Group();
+  headG.name = "bondHead";
+  headG.position.set(0, 1.68 * S, 0);
+  /* round skull */
+  add(headG, new THREE.SphereGeometry(0.2 * S, 16, 14), shiny(SKIN, 25), 0, 0, 0);
+  /* soft jaw (slightly flattened sphere, offset down — not a second full skull) */
+  const jaw = add(headG, new THREE.SphereGeometry(0.16 * S, 14, 12), shiny(SKIN, 25), 0, -0.08 * S, 0.02 * S);
+  jaw.scale.set(1.05, 0.75, 0.95);
+  /* slicked hair cap — hemisphere-ish via scaled sphere, slightly above skull to avoid coplanar flicker */
+  const hairCap = add(headG, new THREE.SphereGeometry(0.205 * S, 14, 10, 0, Math.PI * 2, 0, Math.PI * 0.55), shiny(HAIR, 110), 0, 0.04 * S, -0.01 * S);
+  hairCap.rotation.x = -0.08;
+  /* side hair */
+  for (const s of [-1, 1]) {
+    add(headG, new THREE.SphereGeometry(0.07 * S, 10, 8), shiny(HAIR, 110), 0.16 * s * S, 0.0, -0.02 * S);
+  }
+  /* eyes */
+  for (const s of [-1, 1]) {
+    add(headG, new THREE.SphereGeometry(0.032 * S, 10, 8), shiny(0x14100c, 60), 0.07 * s * S, 0.03 * S, 0.16 * S);
+    const br = add(headG, new THREE.BoxGeometry(0.09 * S, 0.02 * S, 0.02 * S), shiny(HAIR, 60), 0.07 * s * S, 0.08 * S, 0.16 * S);
     br.rotation.z = -0.1 * s;
   }
-  /* THE SNOUT — kept from the pig, front and centre */
