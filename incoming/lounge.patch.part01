pth || !p.screen.w) continue;
+    const scale = Math.max(0.12, Math.min(2.4, p.screen.scale * 2600));
+    raceDrawCar(ctx, p.screen.x, p.screen.y, scale, r.color, r.name, 0);
   }
 
-  /* speed stripe HUD bottom of canvas */
-  ctx.fillStyle = "rgba(0,0,0,0.45)";
+  /* bottom HUD strip on canvas */
+  ctx.fillStyle = "rgba(0,0,0,0.5)";
   ctx.fillRect(0, height - 36, width, 36);
   ctx.fillStyle = "#8fd1ff";
   ctx.font = "bold 14px system-ui";
